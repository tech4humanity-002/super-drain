export function readJsonBody(req,{maxBytes=5_000_000}={}){return new Promise((resolve,reject)=>{const chunks=[];let bytes=0,tooLarge=false;req.on("data",chunk=>{if(tooLarge)return;const b=Buffer.isBuffer(chunk)?chunk:Buffer.from(chunk);bytes+=b.length;if(bytes>maxBytes){tooLarge=true;chunks.length=0;return}chunks.push(b)});req.on("end",()=>{if(tooLarge)return reject(new Error("request too large"));try{const raw=Buffer.concat(chunks,bytes).toString("utf8");resolve(raw?JSON.parse(raw):{})}catch(e){reject(e)}});req.on("error",reject)})}
